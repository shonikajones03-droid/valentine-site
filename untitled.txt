<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Secret Valentine 💘</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<!-- Confetti -->
<script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

<!-- QR Code -->
<script src="https://cdn.jsdelivr.net/npm/qrcodejs/qrcode.min.js"></script>

<style>
body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: linear-gradient(135deg, #ffdde1, #ee9ca7);
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
  text-align: center;
  color: #333;
}

.hidden {
  display: none;
}

button {
  padding: 12px 24px;
  font-size: 18px;
  border: none;
  border-radius: 30px;
  cursor: pointer;
  margin: 10px;
}

input {
  padding: 10px;
  font-size: 16px;
  border-radius: 20px;
  border: none;
  text-align: center;
}

#noBtn {
  position: absolute;
}

#countdown {
  font-size: 18px;
  margin: 15px 0;
  font-weight: bold;
}

.loader p {
  font-size: 18px;
  animation: blink 1.5s infinite;
}

@keyframes blink {
  50% {
    opacity: 0.5;
  }
}
</style>
</head>

<body>

<!-- LOADING SCREEN -->
<div id="loading" class="loader">
  <h1>💘 Loading Romance...</h1>
  <p id="loadingText">Consulting Cupid…</p>
</div>

<!-- PASSWORD PAGE -->
<div id="login" class="hidden">
  <h2>🔒 This page is only for you</h2>
  <input type="password" id="password" placeholder="Enter the secret word">
  <br><br>
  <button onclick="checkPassword()">Unlock 💖</button>
  <p id="error"></p>
</div>

<!-- QR PAGE -->
<div id="qrPage" class="hidden">
  <h2>👀 Scan this QR code</h2>
  <p>It leads to something important…</p>
  <div id="qrcode"></div>
  <br>
  <button onclick="showQuestion()">I scanned it 😌</button>
</div>

<!-- QUESTION PAGE -->
<div id="question" class="hidden">
  <h1>Will you be my Valentine? 💘</h1>

  <div id="countdown">
    ⏳ Counting down to Valentine’s Day...
  </div>

  <button onclick="confirmYes()">YES 💖</button>
  <button id="noBtn">No 💔</button>
</div>

<!-- FINAL PAGE -->
<div id="final" class="hidden">
  <h1>🎉 IT’S OFFICIAL 🎉</h1>
  <p>You’re my Valentine and I’m ridiculously happy 😭❤️</p>
</div>

<script>
/* ---------------- LOADING SCREEN ---------------- */
const loadingMessages = [
  "Consulting Cupid…",
  "Charging butterflies…",
  "Aligning hearts…",
  "Almost ready 😌…"
];
let loadIndex = 0;

setInterval(() => {
  document.getElementById("loadingText").innerText =
    loadingMessages[loadIndex];
  loadIndex = (loadIndex + 1) % loadingMessages.length;
}, 1200);

setTimeout(() => {
  document.getElementById("loading").classList.add("hidden");
  document.getElementById("login").classList.remove("hidden");
}, 5000);

/* ---------------- PASSWORD CHECK ---------------- */
function checkPassword() {
  const pass = document.getElementById("password").value.toLowerCase();
  if (pass === "heart") {
    document.getElementById("login").classList.add("hidden");
    document.getElementById("qrPage").classList.remove("hidden");

    new QRCode(document.getElementById("qrcode"), {
      text: "You knew this was coming 😌💘",
      width: 180,
      height: 180
    });
  } else {
    document.getElementById("error").innerText =
      "Wrong 😏 Try again.";
  }
}

/* ---------------- SHOW QUESTION + TIMER ---------------- */
function showQuestion() {
  document.getElementById("qrPage").classList.add("hidden");
  document.getElementById("question").classList.remove("hidden");
  startCountdown();
}

/* ---------------- ESCAPING NO BUTTON ---------------- */
const noBtn = document.getElementById("noBtn");
noBtn.addEventListener("mouseover", () => {
  noBtn.style.left = Math.random() * 80 + "%";
  noBtn.style.top = Math.random() * 80 + "%";
});

/* ---------------- FUNNY CONFIRMATIONS ---------------- */
let step = 0;
const confirmations = [
  "WAIT 😳 Are you sure?",
  "Like… emotionally sure?",
  "This decision includes cuddles 😌",
  "No refunds. No exchanges.",
  "FINAL ANSWER??? 💘"
];

function confirmYes() {
  if (step < confirmations.length) {
    alert(confirmations[step]);
    step++;
  } else {
    finish();
  }
}

/* ---------------- FINAL CELEBRATION ---------------- */
function finish() {
  document.getElementById("question").classList.add("hidden");
  document.getElementById("final").classList.remove("hidden");

  confetti({
    particleCount: 200,
    spread: 100,
    origin: { y: 0.6 }
  });
}

/* ---------------- COUNTDOWN TIMER ---------------- */
function startCountdown() {
  const countdownEl = document.getElementById("countdown");

  const now = new Date();
  let year = now.getFullYear();
  let valenti
